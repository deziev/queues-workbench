{
  "name": "queues-workbench",
  "version": "1.0.0",
  "homepage": "https://github.com/deziev/queues-workbench#readme",
  "repository": {
    "type": "git",
    "url": "https://github.com/deziev/queues-workbench.git"
  },
  "description": "Playing around with queues",
  "main": "dist/app.js",
  "scripts": {
    "start": "node dist/server.js",
    "server": "node dist/server.js",
    "client": "node dist/client.js",
    "clean": "rm -rf ./dist",
    "compile": "npm run clean && ./node_modules/.bin/tsc",
    "compile-dev": "npm run clean && ./node_modules/.bin/tsc --watch",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "queue",
    "concurrency",
    "jobs",
    "tasks"
  ],
  "author": "deziev",
  "license": "ISC",
  "dependencies": {
    "@types/node": "^14.14.28",
    "typescript": "^4.1.5"
  },
  "devDependencies": {
    "prettier": "2.2.1"
  }
}
